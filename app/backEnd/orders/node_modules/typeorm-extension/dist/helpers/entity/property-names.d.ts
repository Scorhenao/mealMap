import type { ObjectLiteral } from 'rapiq';
import { Repository } from 'typeorm';
import type { DataSource, EntityTarget } from 'typeorm';
/**
 * Get (relation-) property names of a given entity.
 *
 * @experimental
 * @param input
 * @param dataSource
 */
export declare function getEntityPropertyNames<T extends ObjectLiteral>(input: EntityTarget<T> | Repository<T>, dataSource?: DataSource): Promise<string[]>;
