import type { DatabaseCreateContext } from './type';
/**
 * Create database for specified driver in ConnectionOptions.
 *
 * @throws DriverError
 * @throws OptionsError
 *
 * @param context
 */
export declare function createDatabase(context?: DatabaseCreateContext): Promise<any>;
